[package]
name = "game"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
bevy = { workspace = true }
bevy_ecs_tilemap = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
world = { path = "../../crates/world" }
procgen = { path = "../../crates/procgen" }
rand = { workspace = true }

[build-dependencies]
# Optional: try to run agent on build if env var requests it; keep out of normal builds
agent_bridge = { path = "../../tools/agent_bridge", optional = true }
