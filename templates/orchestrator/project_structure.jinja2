<!-- SYSTEM_PROMPT -->
You are generating the complete Godot project structure for Dragon's Labyrinth.

Based on all the generated systems, create:
1. A comprehensive directory structure
2. All boilerplate files (project.godot, scenes, resources, etc.)
3. Configuration files (.gitignore, README.md, etc.)
4. Default assets (icon.svg, audio bus layout, etc.)

Game Details:
- Name: {{ game_name }}
- Description: {{ game_description }}
- World Size: {{ world_size }}
- Difficulty Options: {{ difficulty_options | join(', ') }}
- Starting Classes: {{ starting_classes | join(', ') }}

{% if previous_systems %}
Context from ALL generated systems:
{% for system_name, context in previous_systems.items() %}
{{ system_name }}:
  - Generated files: {{ context.files | join(', ') }}
  {% if context.autoloads %}
  - Autoloads: {{ context.autoloads | join(', ') }}
  {% endif %}
  {% if context.scenes %}
  - Required scenes: {{ context.scenes | join(', ') }}
  {% endif %}
  {% if context.resources %}
  - Resource types: {{ context.resources | join(', ') }}
  {% endif %}
{% endfor %}
{% endif %}

Generate ALL files needed for a complete Godot project. Consider:
- Every system needs appropriate directories for its files
- Create a logical, organized structure
- Include all necessary Godot configuration files
- Add helpful default content where appropriate
- Structure should support all 18 game systems

For each file, use the format:
<!-- FILE: path/to/file -->
file content here
<!-- END FILE -->

Start with these essential files:
- project.godot (with ALL systems integrated)
- icon.svg (Dragon's Labyrinth themed)
- default_bus_layout.tres
- Main.tscn
- Main.gd
- README.md
- .gitignore
- .gdignore files where needed

Then add any other files needed for a complete, runnable project.
